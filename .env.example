# LINE Bot Configuration
# Get these from your LINE Developer Console
LINE_CHANNEL_ACCESS_TOKEN=your_line_channel_access_token_here
LINE_CHANNEL_SECRET=your_line_channel_secret_here
LINE_CHANNEL_ID=your_line_channel_id_here

# Azure OpenAI Configuration
# Get these from your Azure OpenAI resource
AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
AZURE_OPENAI_ENDPOINT=https://thaibev-azure-subscription-ai-foundry.cognitiveservices.azure.com
AZURE_OPENAI_API_VERSION=2024-02-15-preview
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4-1-nano

# Application Configuration
DEBUG=True
LOG_LEVEL=INFO
SESSION_SECRET=your_secret_key_for_flask_sessions

# Conversation Management (Optional - defaults provided)
MAX_MESSAGES_PER_USER=100
MAX_TOTAL_CONVERSATIONS=1000

# Instructions:
# 1. Copy this file to .env
# 2. Fill in your actual credentials
# 3. Never commit .env to version control
# 4. For Replit, add these as Secrets in the Secrets tab

# LINE Developer Console Setup:
# 1. Go to https://developers.line.biz/console/
# 2. Create a new channel or select existing one
# 3. In Messaging API tab, get:
#    - Channel Access Token (long-lived)
#    - Channel Secret
#    - Channel ID (Basic settings tab)
# 4. Set webhook URL to: https://your-replit-name.your-username.repl.co/webhook
# 5. Enable webhook and disable auto-reply in channel settings

# Azure OpenAI Setup:
# 1. Go to Azure Portal
# 2. Find your Azure OpenAI resource
# 3. In Keys and Endpoint, get:
#    - API Key (Key 1 or Key 2)
#    - Endpoint URL
# 4. In Model deployments, note your deployment name
# 5. API version should match your Azure OpenAI service version
